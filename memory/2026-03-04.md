
## 2026-03-04 - FiscalNext Development Session

### Completed Today

**1. Fixed Audit Logs Page**
- Created `audit_logs` table in PostgreSQL
- Fixed API endpoint mismatch (`/audit-logs` → `/audit`)
- Fixed snake_case to camelCase transformation
- Removed problematic `logView` call that was breaking responses

**2. Fixed Reports Multi-Currency**
- Reports now use same calculation as dashboard (product currency from items)
- Added currency tabs (ALL, EUR, USD) 
- Added quick date filters (Today, Last 7 Days, Last 30 Days, Last Year)
- Fixed byCurrency data calculation

**3. Fixed Fiscal Receipts Page**
- Fixed `toFixed` error on total (wrapped in `Number()`)

**4. Added Offline Mode**
- Created `lib/offline/offlineDb.ts` - IndexedDB wrapper
- Created `lib/offline/syncService.ts` - Queue & sync transactions
- Created `components/ui/OfflineIndicator.tsx` - Shows online/offline status
- Transactions queue locally when offline, sync when back online

**5. Added Albanian Language (i18n)**
- Created `lib/i18n/translations.ts` - 200+ translations (en/sq)
- Created `lib/i18n/LanguageProvider.tsx` - Context & hooks
- Added `LanguageSwitcher` component in header (🇦🇱 SQ / 🇬🇧 EN)
- Default language: Albanian
- Updated Sidebar to use translations

**6. Added WhatsApp Receipt Sharing**
- Created `lib/utils/receiptGenerator.ts` - Formats receipts for WhatsApp
- Added WhatsApp button to POS success modal
- Receipt includes: business info, items, totals, fiscal code, Albanian text

**7. Started WhatsApp API Integration (Twilio)**
- Created `api/services/whatsapp.service.ts` - Twilio WhatsApp API
- Created `api/routes/whatsapp.ts` - API endpoints
- Added `whatsappApi` to frontend
- Env vars needed: `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_NUMBER`

### In Progress
- Auto-send receipt via WhatsApp when transaction completes (adding customer phone input to POS)

### Project Location
- Monorepo: `/Users/admin/.openclaw/workspace/programfiskalizimi/code/fiscalnext-monorepo`
- GitHub: `git@github.com:Marliish/programfiskalizimi.git`

### Key URLs (Local Dev)
- Admin Dashboard: http://localhost:3001
- POS: http://localhost:3001/pos
- API: http://localhost:5000
- API Docs: http://localhost:5000/docs

### Notes
- Deleted unused pages: users, promotions, loyalty
- Removed unused `src/` folder with antd dependencies
- Fixed various TypeScript errors for build to pass
